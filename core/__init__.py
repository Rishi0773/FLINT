"""FLINT core engine package.

Modules:
    capture_engine  — frame-difference screen/camera capture with token caching
    async_pipeline  — worker-thread job pipeline + event bus
    log_interceptor — stdout/stderr tee feeding the in-app terminal
    ws_listener     — WebSocket command listener for remote (smartphone) clients
"""
